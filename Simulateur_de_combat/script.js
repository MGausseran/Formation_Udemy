class Character {
    constructor(name, health, might) {
        this.name=name;
        this.health=health;
        this.might=might;
    }

    getStats() {
        
        stats = document.getElementsByClassName("stats");
        stats.createElement('p');
    }
}